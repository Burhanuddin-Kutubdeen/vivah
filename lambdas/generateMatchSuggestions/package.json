{
  "name": "generate-match-suggestions-lambda",
  "version": "1.0.0",
  "description": "Lambda function to generate match suggestions for users (scheduled).",
  "main": "index.js",
  "type": "commonjs",
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "zip-local": "^0.3.5"
  },
  "scripts": {
    "package": "rm -f deployment-package.zip && zip -r deployment-package.zip . -x '*.git*' '*node_modules/aws-sdk/*' '*node_modules/zip-local/*'"
  }
}
